import React from 'react';

export const SettingsPage: React.FC = () => {
    return (
        <div>
            <h2 className="text-2xl font-bold mb-4">Settings</h2>
            <p className="text-text-muted">App configuration and sync status.</p>
        </div>
    );
};
